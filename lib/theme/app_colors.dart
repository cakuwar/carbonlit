import 'package:flutter/material.dart';

/// App-wide color constants for the Carbon Calculator UI.
class AppColors {
  AppColors._();

  /// Primary dark green — used for AppBar, buttons, and accents.
  static const Color primaryGreen = Color(0xFF115925);

  /// Slightly lighter green for selected states.
  static const Color selectedGreen = Color(0xFF1B5E20);

  /// Light green for backgrounds, pills, and highlights.
  static const Color lightGreen = Color(0xFFE8F5E9);

  /// Soft green for borders and subtle backgrounds.
  static const Color softGreen = Color(0xFFC8E6C9);

  /// Accent green for emission values.
  static const Color accentGreen = Color(0xFF2E7D32);

  /// Dark text.
  static const Color darkText = Color(0xFF212121);

  /// Medium grey for subtitles and unselected icons.
  static const Color grey = Color(0xFF9E9E9E);

  /// Light grey for card backgrounds.
  static const Color lightGrey = Color(0xFFF5F5F5);
}
